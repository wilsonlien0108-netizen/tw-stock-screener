# 手機部署指南 — 讓家人朋友用手機直接開

兩種方式並行：**雲端版**（家人日常用，固定網址、你的電腦關機也能用）＋
**通道版**（臨時分享全功能，含盤中即時）。

---

## 方式一：雲端版（推薦家人日常用）

架構：你的電腦每晚更新資料 → 自動上傳到你的 GitHub → Streamlit Cloud
免費雲端跑網頁 → 家人手機開固定網址。全程免費。

### 一次性設定（約 30 分鐘，只有你要做）

**Step 1：註冊 GitHub 並建立儲存庫**
1. 到 [github.com](https://github.com) 註冊帳號（免費）
2. 右上角 ➕ → New repository → 名稱填 `tw-stock-screener`，
   選 **Public**，按 Create repository

**Step 2：把程式碼推上去**（在專案資料夾開 PowerShell，逐行執行）
```powershell
git init
git add .
git commit -m "init"
git branch -M main
git remote add origin https://github.com/你的帳號/tw-stock-screener.git
git push -u origin main
```
（`data/` 與 `runtime/` 已被 .gitignore 排除，不會上傳）
推送時會跳出 GitHub 登入視窗，登入即可。

**Step 3：建立資料上傳用的 Token**
1. GitHub 右上頭像 → Settings → Developer settings →
   Personal access tokens → **Tokens (classic)** → Generate new token (classic)
2. Note 隨便填、Expiration 選 No expiration、勾選 **repo** 整組權限
3. Generate 後**立刻複製** token（只顯示一次）

**Step 4：在篩選器設定自動發佈**
1. 開啟篩選器 → 「資料與設定」→「☁️ 雲端發佈」
2. 填入 `你的帳號/tw-stock-screener` 與剛才的 token → 儲存 → **立即發佈一次**
3. 之後每晚排程更新完會自動上傳，不用再管

**Step 5：部署 Streamlit Cloud**
1. 到 [share.streamlit.io](https://share.streamlit.io) 用 GitHub 帳號登入
2. Create app → 選你的 `tw-stock-screener` 儲存庫、branch `main`、
   Main file path 填 `app.py`
3. Advanced settings → **Secrets** 貼上（換成你的帳號）：
   ```toml
   data_url = "https://github.com/你的帳號/tw-stock-screener/releases/download/data/screener.db"
   ```
4. Deploy！約 3 分鐘後得到固定網址 `https://xxxx.streamlit.app`

**Step 6：傳網址給家人**（見最下方「家人手機設定」）

### 日常維運
- 你的電腦平日 20:30 排程更新後自動上傳，雲端 30 分鐘內自動同步
- 程式碼有更新時：`git add . ; git commit -m "update" ; git push`，
  雲端會自動重新部署
- 雲端版限制：無盤中即時報價（證交所擋海外 IP）、無推播、
  概念股清單請在你的電腦上維護

---

## 方式二：通道版（臨時分享全功能）

雙擊 **`雲端通道啟動.bat`**：
- 首次會自動下載 Cloudflare 官方通道程式（約 17MB）
- 視窗中出現 `https://XXXX.trycloudflare.com` 網址後，傳給家人即可
- **全功能**：含盤中即時報價（因為實際跑在你的電腦上）
- 限制：你的電腦要開著；每次重啟網址會變，要重傳

---

## 家人手機設定（一次 30 秒）

把網址傳給家人（LINE 即可），請他們：

**iPhone**：Safari 開啟網址 → 分享按鈕 → **加入主畫面**
**Android**：Chrome 開啟網址 → 右上 ⋮ → **加到主畫面**

之後手機桌面就有一個「台股篩選器」圖示，點開就像 App 一樣用。
手機上建議先點左上角 ✕ 收合側邊欄看結果，要調條件再展開。
